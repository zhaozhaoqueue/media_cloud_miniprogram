.page {
  min-height: 100vh;
  background: #f7f3eb;
  color: #1f1f1f;
  padding: 24px 18px 32px;
}

.hero {
  background: radial-gradient(circle at 20% 20%, #fff6df 0%, #f0e4cf 45%, #e4d2b1 100%);
  border-radius: 20px;
  padding: 18px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
}

.title {
  font-size: 22px;
  font-weight: 700;
  letter-spacing: 0.2px;
}

.subtitle {
  margin-top: 4px;
  color: #5e523d;
  font-size: 13px;
  line-height: 1.5;
}

.hero-actions {
  margin-top: 10px;
  display: flex;
  gap: 12px;
}

.section {
  margin-top: 24px;
}

.section-title {
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 12px;
}

.card-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.card {
  position: relative;
  background: #ffffff;
  border-radius: 16px;
  padding: 16px;
  box-shadow: 0 6px 16px rgba(0, 0, 0, 0.06);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.space-delete {
  position: absolute;
  right: 10px;
  top: 8px;
  width: 18px;
  height: 18px;
  line-height: 18px;
  text-align: center;
  border-radius: 50%;
  color: #c15b4f;
  border: 1px solid #edc6be;
  background: #ffffff;
  font-size: 13px;
  z-index: 2;
}

.space-delete-disabled {
  color: #b9b2a4;
  border-color: #e7e1d2;
  background: #f5f1e8;
}

.card-left {
  flex: 1;
  min-width: 0;
}

.card-title {
  font-size: 16px;
  font-weight: 600;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-meta {
  margin-top: 6px;
  color: #6f6f6f;
  font-size: 12px;
}

.card-cover {
  width: 68px;
  height: 68px;
  border-radius: 12px;
  overflow: hidden;
  background: #f3ece0;
  flex-shrink: 0;
}

.cover-image {
  width: 100%;
  height: 100%;
}

.cover-fallback {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #8a7d68;
  font-size: 11px;
}

button.primary {
  background: #1f1f1f;
  color: #ffffff;
  border-radius: 999px;
  flex: 1;
}

button.ghost {
  background: #ffffff;
  color: #1f1f1f;
  border-radius: 999px;
  border: 1px solid #e1d9c9;
  flex: 1;
}

.join-panel {
  margin-top: 14px;
  background: #ffffff;
  border-radius: 18px;
  padding: 16px;
  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.06);
}

.join-title {
  font-size: 14px;
  font-weight: 600;
}

.join-input {
  margin-top: 10px;
  height: 40px;
  border-radius: 12px;
  background: #f5f0e8;
  padding: 0 12px;
  font-size: 14px;
}

.join-actions {
  margin-top: 12px;
  display: flex;
  gap: 10px;
}

.join-actions button.primary,
.join-actions button.ghost {
  flex: 1;
}
